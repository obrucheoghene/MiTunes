interface VerifyUserProps {
    userId?: string,
    secret?: string
}
export const verifyUser = ({userId, secret}:VerifyUserProps ) => {
    if (!userId || !secret) {
        return null
    }
    try {
        
    } catch (error) {
        
    }
}